# ansible
Ansible is only used for software components

##transformation
when the component is visited a play is created.
One difference is that runtime vars are recognized as well.

## orchestration
